{
  "name": "pi-dynamic-models",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.0",
    "vitest": "^4.1.0"
  },
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  }
}
